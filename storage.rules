rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public read so uploaded images display on the live website.
      allow read: if true;
      // Only the logged-in admin can upload or delete files.
      allow write: if request.auth != null;
    }
  }
}
